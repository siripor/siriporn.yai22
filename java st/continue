//*forแบบปกติใส่คำว่า continue กระโดดข้ามเลขที่กำหนดไว้
for(let count = 1 ;count<=10;count++){
    if(count==5)continue;//เป็นการกระโดดข้ามเลข 5 ไป
    console.log(count);
}

console.log("จบโปรแกราม");
